package marius.server.data;

/**
 * Enum  is the corrispective of the {@code Rule} in the anglular interface.
 * Became a numeric enum  for a better integration with the database.
 */
public enum RoleEnum {
        SUPERVISOR,
        WORKER
}
